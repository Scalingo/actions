## Definition of Done (mandatory when present)
- If the PR description contains a "Definition of Done" section, every checklist item under it must be checked (`- [x]`).
- Unchecked items (`- [ ]`) are an error.